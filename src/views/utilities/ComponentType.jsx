import { Card } from '@mui/material';

// project imports
import MainCard from 'ui-component/cards/MainCard';

const ComponentType = () => (
  <MainCard title="Component Type">
    <Card sx={{ overflow: 'hidden' }}>
      
    </Card>
  </MainCard>
);

export default ComponentType;
